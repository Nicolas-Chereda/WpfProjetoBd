<UserControl x:Class="WpfProjetoBd.View.UserControls.TelaDeFilmes"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:WpfProjetoBd.View.UserControls"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    
    <UserControl.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Texto}"/>
        </Style>
     </UserControl.Resources>
    
    <Grid Background="{StaticResource Secundaria}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.Column="0" Background="{StaticResource SecundariaEscura}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition  Width="*"/>
                <ColumnDefinition  Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="TituloBanner">Título</TextBlock>
            <TextBlock x:Name="DescricaoBanner" TextWrapping="Wrap" VerticalAlignment="Center"></TextBlock>

            <Image Grid.Column="1" x:Name="ImagemBanner"/>
        </Grid>

        <ScrollViewer Grid.Row="1"
          VerticalScrollBarVisibility="Hidden"
          HorizontalScrollBarVisibility="Disabled"
          PanningMode="VerticalOnly"
          CanContentScroll="False"
          PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
            <StackPanel>
                <ItemsControl ItemsSource="{Binding}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,0,30" Width="1280">

                                <!-- 🏷️ Título da categoria -->
                                <TextBlock Text="{Binding Nome}"
                               FontSize="18"
                               FontWeight="Bold"
                               Foreground="{StaticResource Texto}"
                               Margin="30,0,0,5"/>


                                <!-- 🎬 Linha de filmes -->
                                <local:ListaDeFilmes x:Name="ListaFilmes" TrailerHover="CardFilme_TrailerHover" TrailerClick="Lista_TrailerClick"/>


                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

    </Grid>
</UserControl>
