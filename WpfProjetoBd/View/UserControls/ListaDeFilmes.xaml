<UserControl x:Class="WpfProjetoBd.View.UserControls.ListaDeFilmes"
            xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
            xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
            xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
            xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
            xmlns:local="clr-namespace:WpfProjetoBd.View.UserControls"
            mc:Ignorable="d" 
            d:DesignHeight="450" d:DesignWidth="800">
   <Grid>
       <Grid.ColumnDefinitions>
           <ColumnDefinition Width="25" />
           <ColumnDefinition Width="*" />
           <ColumnDefinition Width="25" />
       </Grid.ColumnDefinitions>

       <ScrollViewer VerticalScrollBarVisibility="Hidden" HorizontalScrollBarVisibility="Hidden" Grid.Column="1" x:Name="scroll" >
           <ItemsControl ItemsSource="{Binding Trailers}">
               <ItemsControl.ItemsPanel>
                   <ItemsPanelTemplate>
                       <StackPanel Orientation="Horizontal"/>
                   </ItemsPanelTemplate>
               </ItemsControl.ItemsPanel>
               <ItemsControl.ItemTemplate>
                   <DataTemplate>
                        <StackPanel Width="200" Margin="3,0,3,0">
                            <local:CardFilme Loaded="CardCarregado"/>
                        </StackPanel>
                    </DataTemplate>
               </ItemsControl.ItemTemplate>
           </ItemsControl>
       </ScrollViewer>
       <!-- Botões laterais -->
       <Button Content="◀" Width="25" Height="100" VerticalAlignment="Top" Click="Voltar_Click" Background="{StaticResource SecundariaEscura}" BorderThickness="0" Foreground="{StaticResource Texto}" Grid.Column="0" />
       <Button Content="▶" Width="25" Height="100" VerticalAlignment="Top" Click="Avancar_Click" Background="{StaticResource SecundariaEscura}" BorderThickness="0" Foreground="{StaticResource Texto}" Grid.Column="2"/>
   </Grid>
</UserControl>
