<Window x:Class="WpfProjetoBd.PaginaPrincipal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfProjetoBd"
        Title="Página Principal" Height="300" Width="400" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Texto}"/>
        </Style>
    </Window.Resources>
    
    <Grid Background="{StaticResource Secundaria}">
        <Grid.RowDefinitions>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
        </Grid.ColumnDefinitions>

        <Grid Grid.Row="0" Grid.Column="0" Background="{StaticResource SecundariaEscura}">
            <Grid.ColumnDefinitions>
                <ColumnDefinition  Width="*"/>
                <ColumnDefinition  Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock x:Name="TituloBanner">Título</TextBlock>
            <TextBlock x:Name="DescricaoBanner" TextWrapping="Wrap" VerticalAlignment="Center"></TextBlock>

            <Image Grid.Column="1" x:Name="ImagemBanner"/>
        </Grid>

        <ScrollViewer Grid.Row="1"
              VerticalScrollBarVisibility="Hidden"
              HorizontalScrollBarVisibility="Disabled"
              PanningMode="VerticalOnly"
              CanContentScroll="False"
              PreviewMouseWheel="ScrollViewer_PreviewMouseWheel">
            <StackPanel>
                <ItemsControl ItemsSource="{Binding}">
                    <ItemsControl.ItemTemplate>
                        <DataTemplate>
                            <StackPanel Margin="0,0,0,30">

                                <!-- 🏷️ Título da categoria -->
                                <TextBlock Text="{Binding Nome}"
                                   FontSize="18"
                                   FontWeight="Bold"
                                   Foreground="{StaticResource Texto}"
                                   Margin="30,0,0,5"/>
                                

                                <!-- 🎬 Linha de filmes -->
                                <local:CardFilme Loaded="CardFilme_Loaded"/>

                            </StackPanel>
                        </DataTemplate>
                    </ItemsControl.ItemTemplate>
                </ItemsControl>
            </StackPanel>
        </ScrollViewer>

    </Grid>
</Window>
