<Window x:Class="WpfProjetoBd.PaginaPrincipal"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:local="clr-namespace:WpfProjetoBd"
        Title="Página Principal" Height="720" Width="1380" WindowStartupLocation="CenterScreen">

    <Window.Resources>
        <Style TargetType="TextBlock">
            <Setter Property="Foreground" Value="{StaticResource Texto}"/>
        </Style>
    </Window.Resources>

	<Grid Background="{StaticResource Secundaria}">
		<Grid.ColumnDefinitions>
			<ColumnDefinition Width="50"/>
			<ColumnDefinition Width="*"/>
		</Grid.ColumnDefinitions>
		
		<!-- Menu Lateral -->
		<Grid Grid.Column="0">
			<Grid.RowDefinitions>
				<RowDefinition Height="*"/>
				<RowDefinition Height="100"/>
				<RowDefinition Height="100"/>
				<RowDefinition Height="*"/>
			</Grid.RowDefinitions>

			<Button Grid.Row="1" Content="Início" Margin="5" Click="BtnInicio_Click"/>
			<Button Grid.Row="2" Content="Amigos" Margin="5" Click="BtnAmigos_Click"/>
		</Grid>

		<!-- Conteúdo Principal -->
		<ContentControl Grid.Column="1" x:Name="ConteudoPrincipal"/>
		
	</Grid>
</Window>
